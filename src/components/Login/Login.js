import React from 'react';

const Login = () => {
    return (
        <div>
            <h4>Please Login with</h4>
            <button>Login using Google</button>
        </div>
    );
};

export default Login;